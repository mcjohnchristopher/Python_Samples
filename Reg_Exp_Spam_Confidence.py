import re
hand = open('Filename.txt')
numlist = list()
for lind in hand:
	line = line.rstrip('abc: [0-9.]+)',line)
	if len(stuff) = !1 : continue
	num = float(stuff[0])
	numlist.append(num)
print 'Maximum:',max(numlist)