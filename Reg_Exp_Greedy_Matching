# Greedy Matching
import re
x = 'From: Using the: character'
y = re.findall('^F.+:',x)
print y

# Non Greedy Matching
import re
x = 'From: Using the: character'
y = re.findall('^F.+?:',x)
print y
